package app.command
import scala.annotation.tailrec

object Diff {}
/*
  def makeDiff(file: String, newFile: String) {}

  def createMatrix(file: String, newFile: String): List[List[Int]] = {
    val fileSplitted = file.split("\n").toList
    val newFileSplitted = newFile.split("\n").toList

    @tailrec
    def fulfillMatrix(
        file: List[String],
        newFile: List[String],
        res: List[Int] //values of the previous line of matrix
    ): List[List[Int]] = {
      val index = res.size - 1 //nb lines in matrix

    }
    // call with MatrixLine = 1 and matrixCol =
    // If the index is equal to old file size : (need to reset pointer)

  }

 */
